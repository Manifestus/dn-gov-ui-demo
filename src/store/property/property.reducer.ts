export default function propertyReducer(state = []) {
  //     switch (action.type) {
  //       case 'ADD_TODO':
  //         return state.concat([action.text])
  //       default:
  //         return state
  //     }
}
